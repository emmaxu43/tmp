#include <stdio.h>

struct _complex_num_t{
	double real_part;
	double imaginary_part;
	}; 
typedef struct _complex_num_t complex_num_t;

typedef enum _animal_t{
	CAT,
       	FISH,
       	SNAKE,
       	DOG,
       	HORSE
	}; animal_t;
